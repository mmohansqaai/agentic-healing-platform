{
  "name": "locator-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Locator healing agent — modular locator intelligence (Phase 3)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test:unit": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "ai-healing-sdk": "file:../../packages/ai-healing-sdk"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
